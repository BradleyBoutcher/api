components:
  schemas:
    TagResponse:
      type: object
      properties:
        tagText:
          type: string
        ignoreStatus:
          $ref: '../../ignores.yml#/components/schemas/IgnoreResponse'

