#!/bin/bash -e 

# Run from top-level dir
cd "$(dirname "$0")"

if [ ! -z "$(pwd)/destiny.yml" ]; then 
    echo "Please run `bin/bundle` before attempting to use this utility."
    exit 1
fi

docker run --rm \
    -v $(pwd):/work \
    redocly/openapi-cli \
        stats "/work/destiny.yml"
